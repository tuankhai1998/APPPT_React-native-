const initialState = {

}

const searchReducer = (state = initialState, action) => {
    switch (action.type) {
        case 'SEARCH_ADCANCED':
            return state;
        default:
            return state;
    }
}


export default searchReducer;